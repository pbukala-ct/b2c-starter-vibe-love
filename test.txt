Using the browser, run through the following tests.  Fix any found errors and redeploy site

1. Create order - registered user (username: jen@example.com password: 123)
  a. As a registered user, buy a random collection of products and check out
  b. make sure new order shows up under my account -> my orders

1b. Checkout — saved address and real-time cart sync (registered user)
  a. Sign in and add a product to the cart; go to checkout
  b. Verify the shipping address dropdown shows saved addresses
  c. If the cart already has an address matching a saved address, verify that address is pre-selected
  d. Select a different saved address from the dropdown; verify the form hides and the address is applied
  e. Select "Enter new address"; fill in the form fields; verify the shipping address is set on the cart
     after a short pause (check that navigating away and back pre-fills the same address)
  f. Select a shipping method; verify it is set on the cart immediately
  g. Uncheck "Same billing address"; verify billing dropdown appears; select a saved billing address
  h. Complete checkout and verify the order appears under my orders

2. Create order - anonymous
  a. As anonymous (not logged in) user, buy a random collection of products and check out

3. Create subscription
  a. as a registered user, buy a subscription product and check out
  b. make sure the newly created subscription shows up under 'my subscriptions'

4. Create order with multiple ship-to addresses (split shipment)
  a. As a registered user, add at least 2 products to the cart
  b. At checkout, enable "Ship to multiple addresses"
  c. Add a second shipping address
  d. Assign at least one item to each address
  e. Complete checkout
  f. Verify the order appears under my account -> my orders

5. My Account — Profile
  a. Sign in as registered user
  b. Go to My Account → Profile
  c. Change first name to a new value and save
  d. Verify "Profile updated successfully" message appears
  e. Verify the updated name reflects in the account header/nav
  f. Restore the original name and save again

6. My Account — Orders
  a. Sign in as registered user
  b. Go to My Account → Orders
  c. Verify at least one order is listed with order number, date, and total
  d. Click "View details" on any order
  e. Verify the order detail page loads with line items, shipping, and totals

7. My Account — Subscriptions
  a. Sign in as registered user
  b. Go to My Account → Subscriptions
  c. If subscriptions exist:
     - Verify each subscription shows product name, schedule, status badge, and total
     - Click "Pause" on an Active subscription; verify it changes to Paused
     - Click "Resume" on the Paused subscription; verify it returns to Active
  d. If no subscriptions: verify the empty state with "Shop Subscribe & Save" link

8. My Account — Addresses
  a. Sign in as registered user
  b. Go to My Account → Addresses
  c. Click "Add Address"; verify the country field defaults to the current locale's country
  d. Fill in all fields and save
  e. Verify the new address appears in the list with the correct street format
     (US: "123 Main St"; DE/GB: "Musterstraße 1" style — name before number)
  f. Click "Edit" on the new address
  g. Change the city field; verify Default Shipping and Default Billing pills
     can each be toggled independently (one on, both on, neither on)
  h. Save and verify the updated city appears in the card
  i. Click "Remove" on the test address and verify it disappears from the list

9. Product Image Carousel
  a. Navigate to any product detail page that has multiple images
  b. Verify images are displayed in a horizontal carousel (2 visible at once, portrait orientation, no cropping)
  c. Verify left and right arrow buttons are vertically centered over the images
  d. Click the right arrow; verify the carousel scrolls to the next image
  e. Click the left arrow; verify the carousel scrolls back
  f. Verify a thumbnail strip appears below the carousel
  g. Click a thumbnail; verify the carousel scrolls to that image and the thumbnail gets a highlight ring
  h. Verify scrolling the carousel updates the active thumbnail

10. My Account — Payment Methods
  a. Sign in as registered user
  b. Go to My Account → Payment Methods
  c. Click "Add Card", fill in cardholder name, card number (4111 1111 1111 1111), expiry (12/28)
  d. Verify the new Visa card appears in the list showing last 4 digits (1111)
  e. If another card exists, click "Set Default" on the new card and verify the Default badge
  f. Click "Remove" on the new card and verify it disappears from the list

10. Agent Portal — Login and session management
  a. Go to /agent/login
  b. Verify unauthenticated access to /agent/dashboard redirects to /agent/login
  c. Enter invalid credentials; verify "Invalid credentials" error is shown
  d. Enter valid agent credentials (agent@ct.com / 1234!); verify redirect to /agent/dashboard
  e. Verify the agent nav bar shows agent name, role, and a MM:SS countdown timer
  f. Verify countdown timer ticks down in real time
  g. Click "Log out"; verify redirect to /agent/login and session is cleared

11. Agent Portal — Customer lookup
  a. Log in as agent
  b. On the dashboard, type at least 2 characters in the "Search by name" field
  c. Verify a dropdown of matching customers appears within ~300 ms
  d. Select a customer from the dropdown; verify redirect to the customer detail page
  e. Verify the agent nav bar shows the selected customer's full name (not a raw ID)
  f. End the session; go back to dashboard; search by email for a known customer
  g. Verify the result card shows customer name, email, account status, and open order count
  h. Search for a non-existent email; verify "No customer found" message appears
  i. Click "Start customer session"; verify redirect to the customer detail page
  j. Click "End session"; verify redirect back to the dashboard and customer indicator is gone

12. Agent Portal — Active cart view
  a. Log in as agent and start a session for a customer who has an active cart
  b. Verify the cart page shows all line items with SKU, name, quantity, unit price, and total
  c. Verify the cart total is displayed correctly
  d. If a discount code is applied, verify it is shown
  e. If the cart was last modified more than 24 hours ago, verify the staleness warning is shown
  f. Start a session for a customer with no active cart; verify "No active cart" message is shown
  g. (order-placement role) Verify "+ Create cart" button is visible in the no-cart state
  h. (read-only role) Verify no "Create cart" button is shown

13. Agent Portal — Cart editing (order-placement role required)
  a. Log in as agent with order-placement role and start a session for a customer with an active cart
  b. Click "+" on a line item to increase quantity; verify the quantity and total update
  c. Click "−" on a line item to decrease quantity; verify it decrements
  d. Click "Remove" on a line item; verify it disappears from the cart
  e. Enter a valid SKU in the "Add item by SKU" field and click Add; verify the item appears
  f. Enter an invalid SKU; verify an error is shown and the cart is unchanged
  g. Enter a valid discount code and click Apply; verify it appears on the cart
  h. Click "×" on a discount code; verify it is removed
  i. Click "Edit address", fill in all required fields, save; verify the address updates on the cart

14. Agent Portal — Customer address book
  a. Log in as agent with order-placement role and start a session for a customer
  b. Scroll to the "Address Book" section on the customer detail page
  c. Verify all saved addresses are listed with name, street, city, postal code, and country
  d. Click "+ Add address"; fill in all required fields; click "Add address"
  e. Verify the new address appears in the list
  f. Click "Edit" on any address; change the city; click "Save changes"
  g. Verify the updated city appears in the address card
  h. Log in as read-only agent; verify the "Add address" button and "Edit" links are not visible

15. Agent Portal — Cart creation (Phase 3)
  a. Log in as agent with order-placement role and start a session for a customer with NO active cart
  b. Verify the "+ Create cart" button is visible in the "No active cart" empty state
  c. Click "+ Create cart"; verify a loading state is shown briefly
  d. Verify the cart view appears (empty cart with 0 items) without a full page reload
  e. Verify the "Create cart" button is no longer visible
  f. Add an item by SKU and verify the item appears — the cart is usable immediately
  g. (read-only agent) Verify no "Create cart" button is shown for a customer with no active cart
  h. (double-click guard) Click "+ Create cart" and immediately click again; verify only one cart is created

16. Agent Portal — Case-insensitive customer name search (Phase 3)
  a. Log in as agent; go to the dashboard search
  b. Enter "johnson" (all lowercase) in the name search tab
  c. Verify results include customers with lastName "Johnson", "JOHNSON", or mixed case
  d. Enter "jo" (2 characters) and verify results are returned (minimum 2-character threshold met)
  e. Enter "j" (1 character) and verify no results are shown (below minimum)
  f. Enter a partial first name (e.g. "Mar" for "Maria") and verify matching customers appear
  g. Verify the existing email, customerId, and orderId search tabs still work correctly

17. Agent Portal — Order attribution badge (Phase 3)
  a. Log in as agent with order-placement role; start a session for a customer; create or use an existing cart
  b. Add items, set shipping address, and complete agent-initiated checkout
  c. Log in as that customer in the storefront; go to My Account → Orders
  d. Verify the order placed by the agent shows a "CS Agent assisted" badge (indigo pill label)
  e. Verify other orders placed directly by the customer show NO badge
  f. Click into the agent-placed order detail; verify the badge is shown on the detail page too
  g. Verify the badge uses both colour AND text (not colour alone) for accessibility

18. Agent Portal — Checkout with address picker
  a. Log in as agent with order-placement role, start a session for a customer with an active cart and at least one saved address
  b. Click "Place order on behalf of customer"
  c. Verify the confirmation modal shows the customer's saved addresses as radio buttons
  d. Select an address and click "Confirm & place order"
  e. Verify a success message appears with the order number
  f. Verify the cart is now empty / "No active cart" is shown
  g. Start a session for a customer with a cart but no saved addresses and no cart shipping address
  h. Click "Place order on behalf of customer"
  i. Verify the modal shows a message directing the agent to add an address; confirm button is disabled

